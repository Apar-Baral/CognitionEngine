# Future applications

Placeholder for products built on Cognition Engine:

- `pentest-tool/` — LLM pentesting (later)
- `llm-optimizer/` — LLM optimization (later)

Each app gets its own `dna.json` and imports `cognition_engine`.
