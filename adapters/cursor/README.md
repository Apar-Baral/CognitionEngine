# Cursor adapter

Implementation: [`packages/cognition-engine/src/cognition_engine/adapters/cursor.py`](../packages/cognition-engine/src/cognition_engine/adapters/cursor.py)

Install via CLI:

```bash
ce adapter install cursor
# or during start:
ce start --adapter cursor
```

Writes:

- `.cognition/bootstrap.md`
- `.cursor/rules/cognition-engine.mdc`
