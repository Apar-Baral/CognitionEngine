# Claude Code adapter

Implementation: [`packages/cognition-engine/src/cognition_engine/adapters/claude_code.py`](../packages/cognition-engine/src/cognition_engine/adapters/claude_code.py)

Install via CLI:

```bash
ce adapter install claude_code
ce start --adapter claude_code
```

Syncs bootstrap into `CLAUDE.md` and `.cognition/bootstrap.md`.
